<% content_for :head do %>
  <%
    cfg = config[:tech_docs][:tracking_consent_frontend]
    tracking_url = cfg[ENV['ENVIRONMENT'] == 'local' ? :local_url : :remote_url]
  %>

  <!--[if !IE]>-->
  <script
    src="<%= tracking_url %>"
    id="tracking-consent-script-tag"
    data-gtm-container="<%= config[:tech_docs][:tracking_consent_frontend][:gtm_container] %>"
    data-language="en"
  ></script>
  <!--<![endif]-->
<% end %>